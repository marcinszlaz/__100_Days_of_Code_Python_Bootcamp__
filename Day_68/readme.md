### commands
- bash: export FLASK_APP=main.py
- flash shell
- app.config.get('searched value') / app.config prints all available values  
- cmd / power shell: set FLASK_APP=main.py $env:FLASK_APP="main.py"
- flash shell
- app.config.get('searched value') / app.config prints all available values  

### generate good secret key
* import secrets; print(secrets.token_hex())

